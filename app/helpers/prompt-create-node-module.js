'use strict';

/**
 * @type {*[]}
 */
var prompts = [
  {
    default: true,
    message: 'create a node module project',
    name: 'create-node-module',
    type: 'confirm'
  }
];

module.exports = prompts;
